#ifndef DEFINITION_H
#define DEFINITION_H

#include <QLocale>

enum class RSSElement
{
        title,
        link,
        description,
        pubDate,
        language,
        lastBuildDate,
        category
};

#endif // DEFINITION_H
